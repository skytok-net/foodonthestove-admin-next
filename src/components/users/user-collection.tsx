// should be a table of users using a data-table component
// also should have common filter components representing common types of searches
// also should have a search bar

export default function UserCollection() {
  return (
    <div></div>
  );
}